# 时间块记录器 - 数据库设计文档

## 1. 数据库概述

### 1.1 基本信息
- **数据库名称**: time_block
- **数据库类型**: MySQL 8.0+
- **字符集**: utf8mb4
- **排序规则**: utf8mb4_unicode_ci

### 1.2 ER图

```
┌──────────────┐       ┌──────────────┐       ┌──────────────┐
│    users     │       │  categories  │       │ time_blocks  │
├──────────────┤       ├──────────────┤       ├──────────────┤
│ id (PK)      │<──┐  │ id (PK)      │<──  │ id (PK)      │
│ email        │   │  │ user_id (FK) │   │  │ user_id (FK) │
│ password     │   │  │ name         │   │  │ category_id  │
│ name         │   │  │ color        │   │  │ (FK)         │
│ avatar       │   │  │ icon         │   │  │ title        │
│ created_at   │   │  │ sort_order   │   │  │ description  │
│ updated_at   │   │  │ created_at   │   │  │ start_time   │
└──────────────┘   │  └──────────────┘   │  │ end_time     │
       │           │                     │  │ is_completed │
       │           │                     │  │ created_at   │
       │           └─────────────────────┘  │ updated_at   │
       │                                    └──────────────┘
       │                                          │
       │  ┌──────────────┐                        │
       ├─>│  sync_logs   │                        │
       │  ├──────────────┤                        │
       │  │ id (PK)      │                        │
       │  │ user_id (FK) │                        │
       │  │ sync_type    │                        │
       │  │ status       │                        │
       │  │ records_synced│                       │
       │  │ started_at   │                        │
       │  │ completed_at │                        │
       │  │ error_msg    │                        │
       │  └──────────────┘                        │
       │                                          │
       │  ┌──────────────┐                        │
       └─>│ statistics   │                        │
          ├──────────────┤                        │
          │ id (PK)      │                        │
          │ user_id (FK) │                        │
          │ date         │                        │
          │ total_seconds│                        │
          │ category_id  │                        │
          │ category_seconds│                     │
          └──────────────┘
```

---

## 2. 数据表设计

### 2.1 users (用户表)

| 字段名 | 类型 | 长度 | 允许NULL | 默认值 | 说明 |
|--------|------|------|---------|--------|------|
| id | BIGINT | - | NO | AUTO_INCREMENT | 主键ID |
| email | VARCHAR | 255 | NO | - | 邮箱地址 (唯一) |
| password | VARCHAR | 255 | NO | - | 加密密码 (bcrypt) |
| name | VARCHAR | 100 | NO | - | 用户姓名 |
| avatar | VARCHAR | 500 | YES | NULL | 头像URL |
| created_at | DATETIME | - | NO | CURRENT_TIMESTAMP | 创建时间 |
| updated_at | DATETIME | - | NO | CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP | 更新时间 |

**索引**:
- PRIMARY KEY: `id`
- UNIQUE KEY: `idx_email` (`email`)

**DDL**:
```sql
CREATE TABLE users (
  id BIGINT NOT NULL AUTO_INCREMENT,
  email VARCHAR(255) NOT NULL,
  password VARCHAR(255) NOT NULL,
  name VARCHAR(100) NOT NULL,
  avatar VARCHAR(500) NULL,
  created_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
  updated_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  PRIMARY KEY (id),
  UNIQUE KEY idx_email (email)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
```

---

### 2.2 categories (分类表)

| 字段名 | 类型 | 长度 | 允许NULL | 默认值 | 说明 |
|--------|------|------|---------|--------|------|
| id | BIGINT | - | NO | AUTO_INCREMENT | 主键ID |
| user_id | BIGINT | - | NO | - | 所属用户ID |
| name | VARCHAR | 100 | NO | - | 分类名称 |
| color | VARCHAR | 7 | NO | '#1890ff' | 分类颜色 (HEX) |
| icon | VARCHAR | 50 | YES | NULL | 图标名称 |
| sort_order | INT | - | NO | 0 | 排序顺序 |
| created_at | DATETIME | - | NO | CURRENT_TIMESTAMP | 创建时间 |

**索引**:
- PRIMARY KEY: `id`
- INDEX: `idx_user_id` (`user_id`)
- INDEX: `idx_sort_order` (`user_id`, `sort_order`)

**外键**:
- FOREIGN KEY (`user_id`) REFERENCES `users`(`id`) ON DELETE CASCADE

**DDL**:
```sql
CREATE TABLE categories (
  id BIGINT NOT NULL AUTO_INCREMENT,
  user_id BIGINT NOT NULL,
  name VARCHAR(100) NOT NULL,
  color VARCHAR(7) NOT NULL DEFAULT '#1890ff',
  icon VARCHAR(50) NULL,
  sort_order INT NOT NULL DEFAULT 0,
  created_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
  PRIMARY KEY (id),
  INDEX idx_user_id (user_id),
  INDEX idx_sort_order (user_id, sort_order),
  CONSTRAINT fk_category_user FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
```

---

### 2.3 time_blocks (时间块表)

| 字段名 | 类型 | 长度 | 允许NULL | 默认值 | 说明 |
|--------|------|------|---------|--------|------|
| id | BIGINT | - | NO | AUTO_INCREMENT | 主键ID |
| user_id | BIGINT | - | NO | - | 所属用户ID |
| category_id | BIGINT | - | YES | NULL | 分类ID |
| title | VARCHAR | 200 | NO | - | 时间块标题 |
| description | TEXT | - | YES | NULL | 描述/备注 |
| start_time | DATETIME | - | NO | - | 开始时间 |
| end_time | DATETIME | - | NO | - | 结束时间 |
| is_completed | TINYINT | 1 | NO | 0 | 是否完成 (0/1) |
| created_at | DATETIME | - | NO | CURRENT_TIMESTAMP | 创建时间 |
| updated_at | DATETIME | - | NO | CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP | 更新时间 |

**索引**:
- PRIMARY KEY: `id`
- INDEX: `idx_user_id` (`user_id`)
- INDEX: `idx_category_id` (`category_id`)
- INDEX: `idx_time_range` (`user_id`, `start_time`, `end_time`)

**外键**:
- FOREIGN KEY (`user_id`) REFERENCES `users`(`id`) ON DELETE CASCADE
- FOREIGN KEY (`category_id`) REFERENCES `categories`(`id`) ON DELETE SET NULL

**DDL**:
```sql
CREATE TABLE time_blocks (
  id BIGINT NOT NULL AUTO_INCREMENT,
  user_id BIGINT NOT NULL,
  category_id BIGINT NULL,
  title VARCHAR(200) NOT NULL,
  description TEXT NULL,
  start_time DATETIME NOT NULL,
  end_time DATETIME NOT NULL,
  is_completed TINYINT(1) NOT NULL DEFAULT 0,
  created_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
  updated_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  PRIMARY KEY (id),
  INDEX idx_user_id (user_id),
  INDEX idx_category_id (category_id),
  INDEX idx_time_range (user_id, start_time, end_time),
  CONSTRAINT fk_timeblock_user FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE,
  CONSTRAINT fk_timeblock_category FOREIGN KEY (category_id) REFERENCES categories(id) ON DELETE SET NULL
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
```

---

### 2.4 sync_logs (同步记录表)

记录每次 SQLite ↔ MySQL 数据同步操作的详细信息。

| 字段名 | 类型 | 长度 | 允许NULL | 默认值 | 说明 |
|--------|------|------|---------|--------|------|
| id | BIGINT | - | NO | AUTO_INCREMENT | 主键ID |
| user_id | BIGINT | - | NO | - | 所属用户ID |
| sync_type | VARCHAR | 20 | NO | 'manual' | 同步类型: manual/auto |
| status | VARCHAR | 20 | NO | 'pending' | 状态: pending/in_progress/completed/failed |
| records_synced | INT | - | NO | 0 | 同步记录数 |
| started_at | DATETIME | - | NO | CURRENT_TIMESTAMP | 开始时间 |
| completed_at | DATETIME | - | YES | NULL | 完成时间 |
| error_message | TEXT | - | YES | NULL | 错误信息 |

**索引**:
- PRIMARY KEY: `id`
- INDEX: `idx_sync_logs_user` (`user_id`)
- INDEX: `idx_sync_logs_time` (`user_id`, `started_at`)

**DDL**:
```sql
CREATE TABLE IF NOT EXISTS sync_logs (
  id BIGINT NOT NULL AUTO_INCREMENT,
  user_id BIGINT NOT NULL,
  sync_type VARCHAR(20) NOT NULL DEFAULT 'manual' COMMENT 'manual/auto',
  status VARCHAR(20) NOT NULL DEFAULT 'pending' COMMENT 'pending/in_progress/completed/failed',
  records_synced INT NOT NULL DEFAULT 0,
  started_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
  completed_at DATETIME NULL,
  error_message TEXT NULL,
  PRIMARY KEY (id),
  INDEX idx_sync_logs_user (user_id),
  INDEX idx_sync_logs_time (user_id, started_at),
  CONSTRAINT fk_synclog_user FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='同步记录表';
```

---

### 2.5 statistics (统计汇总表)

预计算的日统计数据，提高报表查询效率。

| 字段名 | 类型 | 长度 | 允许NULL | 默认值 | 说明 |
|--------|------|------|---------|--------|------|
| id | BIGINT | - | NO | AUTO_INCREMENT | 主键ID |
| user_id | BIGINT | - | NO | - | 所属用户ID |
| stat_date | DATE | - | NO | - | 统计日期 |
| category_id | BIGINT | - | YES | NULL | 分类ID (NULL=全天总计) |
| total_seconds | INT | - | NO | 0 | 该分类总秒数 |
| block_count | INT | - | NO | 0 | 时间块数量 |
| created_at | DATETIME | - | NO | CURRENT_TIMESTAMP | 创建时间 |

**索引**:
- PRIMARY KEY: `id`
- UNIQUE KEY: `idx_stat_unique` (`user_id`, `stat_date`, `category_id`)
- INDEX: `idx_stat_user_date` (`user_id`, `stat_date`)

**DDL**:
```sql
CREATE TABLE IF NOT EXISTS statistics (
  id BIGINT NOT NULL AUTO_INCREMENT,
  user_id BIGINT NOT NULL,
  stat_date DATE NOT NULL,
  category_id BIGINT NULL,
  total_seconds INT NOT NULL DEFAULT 0,
  block_count INT NOT NULL DEFAULT 0,
  created_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
  PRIMARY KEY (id),
  UNIQUE KEY idx_stat_unique (user_id, stat_date, category_id),
  INDEX idx_stat_user_date (user_id, stat_date),
  CONSTRAINT fk_stat_user FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE,
  CONSTRAINT fk_stat_category FOREIGN KEY (category_id) REFERENCES categories(id) ON DELETE SET NULL
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='统计汇总表';
```

---

## 3. 初始数据

### 3.1 默认分类

```sql
INSERT INTO categories (user_id, name, color, icon, sort_order) VALUES
(1, '工作', '#1890ff', 'briefcase', 1),
(1, '学习', '#52c41a', 'book', 2),
(1, '生活', '#faad14', 'home', 3),
(1, '休息', '#f5222d', 'coffee', 4);
```

---

## 4. 数据库优化建议

### 4.1 索引优化
- `time_blocks`表的`start_time`和`end_time`字段经常一起查询，已创建联合索引
- 按用户分区查询，所有索引都包含`user_id`作为前缀

### 4.2 分区策略
- 对于大数据量，可以考虑按时间对`time_blocks`表进行分区
- 按`user_id`进行分库分表（当用户量达到百万级时）

### 4.3 数据归档
- 定期归档一年前的历史数据到归档表
- 归档数据只保留统计分析所需的基本信息

### 4.4 查询优化
- 使用覆盖索引减少回表
- 对于复杂统计查询，考虑使用物化视图或预计算
- 适当使用Redis缓存热点数据

---

## 5. 数据迁移

### 5.1 迁移工具
- 使用TypeORM migrations管理数据库版本
- 迁移文件命名格式: `YYYYMMDDHHMMSS-description.sql`

### 5.2 备份策略
- 每日全量备份
- 每小时增量备份
- 保留最近30天的备份数据

