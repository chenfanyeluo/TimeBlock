-- ============================================================================
-- Time Block Recorder - 种子数据脚本
-- 说明: 插入默认便签等初始数据
-- 依赖: 必须先执行 init.sql 完成建表
-- ============================================================================

USE `time_block`;

-- --------------------------------------------------------------------------
-- 1. 默认便签数据（绑定 user_id = 1 的用户）
-- --------------------------------------------------------------------------
-- 注意: 此脚本假设已存在 id=1 的用户记录。
--       若尚未创建用户，需先通过注册接口或手动 INSERT 创建用户。
--       颜色值与前端 store.notes 默认值保持一致。

INSERT INTO `notes` (`user_id`, `name`, `color`) VALUES
  (1, '工作', '#409eff'),
  (1, '学习', '#67c23a'),
  (1, '休息', '#e6a23c'),
  (1, '运动', '#f56c6c'),
  (1, '生活', '#9254de'),
  (1, '其他', '#909399');
