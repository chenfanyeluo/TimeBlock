-- ============================================================================
-- Time Block Recorder - 种子数据脚本
-- 说明: 插入默认便签等初始数据
-- 依赖: 必须先执行 init.sql 完成建表
-- ============================================================================

USE `time_block`;

-- --------------------------------------------------------------------------
-- 1. 默认种子用户（id = 1）
-- --------------------------------------------------------------------------
-- 密码为 bcrypt 哈希，明文: TimeBlock@Seed2026
-- 该用户仅用于提供默认便签，建议通过正式注册流程创建真实用户。

INSERT IGNORE INTO `users` (`id`, `email`, `password`, `name`) VALUES
  (1, 'seed@timeblock.local', '$2b$10$sY9JVbdzO94GxiHv2Cgik.v8AU0nu54rPyn3Zs6xlcqYySpYC.wuy', '默认用户');

-- --------------------------------------------------------------------------
-- 2. 默认便签数据（绑定 user_id = 1 的用户）
-- --------------------------------------------------------------------------
-- 颜色值与前端 store.notes 默认值保持一致。

INSERT INTO `notes` (`user_id`, `name`, `color`) VALUES
  (1, '工作', '#409eff'),
  (1, '学习', '#67c23a'),
  (1, '休息', '#e6a23c'),
  (1, '运动', '#f56c6c'),
  (1, '生活', '#9254de'),
  (1, '其他', '#909399');
